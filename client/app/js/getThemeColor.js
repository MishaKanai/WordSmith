export default function getThemeColor(theme) {
    if (theme === "Dark") {
        return "#333366"
    } else if (theme === "Light") {
        return "#ffffcc"
    } else if (theme === "Gold") {
        return "#D4AF37"
    } else {
        return "#553555"
    }
}
